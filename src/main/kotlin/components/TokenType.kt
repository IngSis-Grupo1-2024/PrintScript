package components

enum class TokenType {

    TYPE,
    SEMICOLON,
    ASSIGNATION, // for the =
    DECLARATION, // for the :
    OPERATOR,
    KEYWORD,
    IDENTIFIER,
    COMMENT,
    VALUE,
    FUNCTION,
    PARENTHESIS

}