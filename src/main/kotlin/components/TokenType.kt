package components

enum class TokenType {

    STRING_TYPE,
    NUMBER_TYPE,
    SEMICOLON,
    ASSIGNATION,
    OPERATOR,
    LET_KEYWORD,
    IDENTIFIER,
    FUNCTION_KEYWORD,
    PARENTHESIS,
    COMMENT
}